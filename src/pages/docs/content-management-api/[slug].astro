---
import Page from '../[...rest]/index.astro';
import { buildSidebarItems } from './_buildSidebarItems';

const sidebarItems = await buildSidebarItems();
---

<Page rest={`content-management-api/${Astro.params.slug!}`} additionalSidebarItems={sidebarItems} />
