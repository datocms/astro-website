---
import s from './_style.module.css';

type Props = {
  title: string;
};
---

<div class={s.benefit}>
  <h4 class={s.benefitTitle}>{Astro.props.title}</h4>
  <div class={s.benefitContent}><slot /></div>
</div>
