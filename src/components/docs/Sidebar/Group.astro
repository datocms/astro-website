---
import s from './style.module.css';
import type { Group } from './types';
import Entry from './Entry.astro';

type Props = Group;

const { label, entries } = Astro.props;
---

<div class={s.group}>
  <div class={s.groupName}>{label}</div>
  {entries.map((entry) => <Entry {...entry} />)}
</div>
