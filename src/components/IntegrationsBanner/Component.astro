---
import { Wrapper } from '~/components/Wrapper';
import s from './style.module.css';

interface Props {
  bubbles: { url: string; alt: string | null }[];
}

const { bubbles } = Astro.props;
---

<div class={s.root}>
  <div class={s.background}>
    <div class={s.backgroundInner}>
      {
        bubbles.map((bubble) => (
          <div class={s.backgroundImage}>
            <img loading="lazy" alt={bubble.alt} src={bubble.url} class={s.backgroundImageInner} />
          </div>
        ))
      }
    </div>
  </div>
  <Wrapper>
    <div class={s.foreground}>
      <div class={s.body}>
        <div class={s.title}>
          <slot name="title" />
        </div>
        <div class={s.content}><slot /></div>
      </div>
    </div>
  </Wrapper>
</div>
