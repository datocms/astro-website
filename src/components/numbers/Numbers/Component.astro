---
import { Wrapper } from '~/components/Wrapper';
import s from './style.module.css';
---

<div class={s.root}>
  <Wrapper>
    <div class={s.rootInner}>
      <div class={s.title}>
        <slot name="title" />
      </div>
      <div class={s.rootBlocks}>
        <slot />
      </div>
    </div>
  </Wrapper>
</div>
