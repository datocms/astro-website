---
import s from './style.module.css';

const styles = {
  neutral: s.neutral,
  good: s.good,
  bad: s.bad,
  warning: s.warning,
};

type Props = {
  style: keyof typeof styles;
};

const { style = 'neutral' } = Astro.props;
---

<strong class={styles[style]}><slot /></strong>
