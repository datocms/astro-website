---
import style from './style.module.css';

type Props = {
  as: string;
};

const { as: WrapperElement = 'div' } = Astro.props;
---

<WrapperElement className={style.wrapper}>
  <slot />
</WrapperElement>
